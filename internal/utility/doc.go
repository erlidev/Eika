// Package utility holds the tasks the harness gives a model for itself,
// outside any run: one request with no tools and no history, with thinking
// off, sent to the model the user assigned the task in the utility_models
// setting.
//
// It depends on provider alone. Task names the tasks and Known checks one;
// Model describes the model a task is sent to; Title is the session title
// task.
package utility
